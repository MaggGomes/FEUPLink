module.exports = {
  async removeJsonKeyInArray(key, array) {
    /*
    let finalObj = [];
    for (var i in array) {
      finalObj.push(array[i]['dataValues'][key])
    }

    return finalObj;*/
  },
};
